### 项目名称
行星轨道参数计算
### 项目描述
本项目旨在让你通过使用 numpy 数组处理行星数据，并依据开普勒第三定律和圆周运动公式，计算行星的轨道周期和轨道速度。这将帮助你熟悉 numpy 库在科学计算中的应用，以及理解行星运动的基本物理原理。
## 具体要求
1. **数据准备：** 从 https://nssdc.gsfc.nasa.gov/planetary/factsheet/ 中获取太阳系八大行星的半长轴（单位：m）和质量（单位：kg）数据，使用 numpy 数组来存储这些数据。
2. **导入必要的库：** 在代码中导入 numpy 库用于数组操作，导入 scipy.constants 库获取引力常数 G 和一年的秒数 year 等物理常量。
3. **计算轨道周期：** 根据开普勒第三定律公式 $$P^{2}=\frac{4 \pi^{2}}{G(M + m)} a^{3}$$ 计算行星的轨道周期（单位：s），其中 G 为引力常数，M 为太阳质量（ $M = 1.989\times10^{30}\rm kg$ ），m 为行星质量，a 为行星轨道半长轴。然后将轨道周期转换为年。
4. **计算轨道速度：** 根据圆周运动公式 $v = \frac{2 \pi a}{P}$ 计算行星的轨道速度（单位：m/s），再将其转换为 km/s。
5. **整理结果：** 将计算结果整理成一个字典，字典的键为行星名称，值为一个包含轨道周期（年）和轨道速度（km/s）的元组, 并按照以下表格输出

```
行星轨道参数计算结果
==================================================
行星名称     轨道周期(年)         轨道速度(km/s)     
--------------------------------------------------
水星       0.24            47.88          
金星       0.62            35.03          
地球       1.00            29.79          
火星       1.88            24.13          
木星       11.87           13.06          
土星       29.68           9.62           
天王星      84.18           6.80           
海王星      164.80          5.43           
==================================================

### 提交要求
修改并提交名为planet_orbital_parameters.py 的 Python 文件，实现上述要求，确保代码结构清晰，注释详细。
# 导入 numpy 库，numpy 是 Python 中用于科学计算的基础库，提供了强大的数组操作功能
import numpy as np
# 从 scipy 库中导入 constants 模块，该模块包含了许多常用的物理常量
from scipy import constants

# 从 https://nssdc.gsfc.nasa.gov/planetary/factsheet/ 获取数据
# 创建一个 numpy 数组，用于存储太阳系八大行星的半长轴，单位为米
# 数组中的每个元素对应一颗行星的半长轴数据
semi_major_axes = np.array(
    [5.791e10, 1.082e11, 1.496e11, 2.279e11, 7.783e11, 1.427e12, 2.871e12,
     4.498e12])
# 创建一个 numpy 数组，用于存储太阳系八大行星的质量，单位为千克
# 数组中的每个元素对应一颗行星的质量数据
masses = np.array([3.3011e23, 4.8675e24, 5.97237e24, 6.4171e23,
                  1.8982e27, 5.6834e26, 8.681e25, 1.0241e26])
# 创建一个 numpy 数组，用于存储太阳系八大行星的名称
# 数组中的每个元素对应一颗行星的名称，与前面半长轴和质量数组的元素顺序一一对应
planet_names = np.array(["水星", "金星", "地球", "火星", "木星", "土星", "天王星", "海王星"])

# 定义太阳的质量，单位为千克
# 这是一个常量值，在后续计算中会用到
sun_mass = 1.989e30
# 从 scipy.constants 模块中获取引力常数 G
# 引力常数在开普勒第三定律的计算中是一个重要的参数
G = constants.G
# 从 scipy.constants 模块中获取一年的秒数
# 用于将轨道周期从秒转换为年
year = constants.Julian_year

# 根据开普勒第三定律公式 $P^{2}=\frac{4 \pi^{2}}{G(M + m)} a^{3}$ 计算行星的轨道周期，单位为秒
# 这里使用 numpy 数组的向量化操作，一次性计算出所有行星的轨道周期
# 先计算括号内的值，再开平方得到轨道周期
periods_in_s = np.sqrt(
    (4 * np.pi ** 2 / (G * (sun_mass + masses))) * semi_major_axes ** 3)
# 将轨道周期从秒转换为年
# 通过将以秒为单位的轨道周期除以一年的秒数，得到以年为单位的轨道周期
periods_in_years = periods_in_s / year

# 根据圆周运动公式 $v = \frac{2 \pi a}{P}$ 计算行星的轨道速度，单位为米每秒
# 同样使用 numpy 数组的向量化操作，一次性计算出所有行星的轨道速度
speeds_in_m_per_s = (2 * np.pi * semi_major_axes) / periods_in_s
# 将轨道速度从米每秒转换为千米每秒
# 通过将以米每秒为单位的轨道速度除以 1000，得到以千米每秒为单位的轨道速度
speeds_in_km_per_s = speeds_in_m_per_s / 1000

# 初始化一个空字典，用于存储最终的计算结果
# 字典的键为行星名称，值为一个包含轨道周期（年）和轨道速度（km/s）的元组
results = {}
# 使用 for 循环遍历行星名称数组
# 循环变量 i 表示当前行星在数组中的索引
for i in range(len(planet_names)):
    # 将当前行星的名称作为键，对应的轨道周期（年）和轨道速度（km/s）组成的元组作为值，存入结果字典
    results[planet_names[i]] = (periods_in_years[i], speeds_in_km_per_s[i])

# 输出结果的标题
print("行星轨道参数计算结果")
# 输出分隔线，用于美化输出格式
print("==================================================")
# 输出表头，包含行星名称、轨道周期（年）和轨道速度（km/s）的信息
print("行星名称     轨道周期(年)         轨道速度(km/s)     ")
# 输出分隔线，用于区分表头和数据
print("--------------------------------------------------")
# 使用 for 循环遍历行星名称数组
# 依次输出每颗行星的名称、轨道周期（年）和轨道速度（km/s）
for name in planet_names:
    # 使用格式化字符串输出结果，左对齐行星名称，保留两位小数输出轨道周期和轨道速度
    print(f"{name:<10}{results[name][0]:<20.2f}{results[name][1]:<10.2f}")
# 输出分隔线，用于结束结果输出
print("==================================================")

